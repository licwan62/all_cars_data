# 尺码簇分析

本项目只读 `../source/尺码分析.csv`，并使用 `../尺码计算/input/尺码匹配规则.csv` 复算当前规则，分析尺码边界、销量影响和可调整分组。运行：

```powershell
python 尺码簇分析/analyze_size_clusters.py
```

新的分析工作簿、报告和调整表统一写到 `尺码簇分析/output/`，不会修改 `source`。根目录现有的同名历史文件保留为旧快照，不再作为默认输出目标。分析结论需要先人工转化为“尺码计算”的项目规则，再重新生成 `pandas_output.csv` 候选；本项目不直接发布真源。
