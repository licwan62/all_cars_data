# 发布报告

- 节点：`size-compression`（`A2.压缩尺寸信息`）
- 版本：`20260923_12`
- 发布时间：`2026-09-23T19:18:16+08:00`
- 工作描述：size-compression 输出刷新。

## 交付物与变更

### `压缩尺码表_US.csv`

- 内容已变化：1740 行 → 1674 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。

### `压缩尺码表_US_有损.csv`

- 内容已变化：1396 行 → 1323 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。

### `压缩尺码表_EU.csv`

- 内容已变化：4991 行 → 4926 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。

### `压缩尺码表_EU_有损.csv`

- 内容已变化：4676 行 → 4616 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。

### `压缩尺码表_RU.csv`

- 内容已变化：7455 行 → 7298 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。

### `压缩尺码表_RU_有损.csv`

- 内容已变化：7136 行 → 7060 行。
- CSV 内容已变化；缺少可唯一定位的 `DIMENSION-ID`，未生成行级差异。
