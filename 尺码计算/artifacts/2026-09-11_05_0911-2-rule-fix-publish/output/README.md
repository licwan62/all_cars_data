# 0911.2 规则改错发布

发布日期：2026-09-11

本批次使用 `尺码计算/rules/0911.2-改错.csv` 重新计算并发布 `public/全量数据.csv`，同时发布同次 `public/尺码匹配规则.csv` 与 `public/尺码匹配报告.json`。

## 结果

- 总记录 4,354，DIMENSION-ID 全量唯一。
- 已匹配 4,169 条；无可用尺码 112 条；数据不全 73 条。
- 相对发布前结果共有 237 条变化，且只涉及 `自动尺码` 与 `自动长度余量`。
- 131 条由 `3XXXXL-0` 调整为 `3XXXL-0`。
- 106 条由 `3XXXL-0` 调整为 `3XXL`。
- 变化记录销量合计 22,750,915。
- 车形、前后宽、参考侧高、插片指数和等效长均未变化。

`发布前全量数据.csv`、`发布前尺码匹配规则.csv` 和 `发布前尺码匹配报告.json` 是本轮回滚快照。
