# 车形分类规则入口

车身号定义的唯一真源是仓库 `public/参考尺寸计算.csv`，判定流程见 `AGENT.md`。历史批次的 `reference.csv` 仅作快照，不作为当前规则源。

研究默认采用 `AGENT.md` 第 5.2 节的方案 B（分层证据研究），并执行覆盖收益优先、代际/实际车身分支复用、抽样校准和增量交付。高风险边界或冲突项升级为方案 A；方案 C 只生成候选，不得直接发布。
