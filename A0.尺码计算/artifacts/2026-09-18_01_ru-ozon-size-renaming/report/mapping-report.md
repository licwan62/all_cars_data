# RU Ozon 新尺码映射

- 为 `data/ru/ozon映射.csv` 新增 `新尺码` 列，并依照 `data/0917新旧尺码对应/新旧尺码对应.csv` 映射。
- 为 `data/ru/0917.1-新命名.csv` 新增 `Ozon尺码`、`发货尺码` 列，并以新尺码回填。
- `2XXL-525`、`3XXXL-0`、`3XXXXL-0`、`PK-XL+`、`3XXXL`、`YM+` 在给定对照表中没有映射，保持空白。
- `2L+` 被对照表映射为 `2L`，但新命名表没有 `2L` 行，因此没有可回填目标；需确认该项应改为 `2L+` 或补充 `2L`。
