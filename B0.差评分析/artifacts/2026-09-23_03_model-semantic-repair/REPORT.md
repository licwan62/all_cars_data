# 车型语义回填报告

## 修改内容

- 根据英文与翻译的完整语义，为 45 个明确提及车辆品牌、车型的差评主键建立修复映射。
- `00.差评分析汇总.csv` 本次新增回填 44 条；此前已处理的 `B0-RAW-9846B68FA382` 纳入映射，保证后续可复现。
- 同步更新 `01.差评分析精选.csv` 中命中映射的 5 条车型记录。
- 同时填写车型修复依据、提取年份和信息提取依据。

## 判定边界

- 只写品牌而未写车型的记录不回填。
- 同时提到多个候选车型、无法确定实际车辆的记录不回填。
- 评论中的购买日期、生产日期不作为车型年份。
- 明确的中英文车型别名统一为常用英文车型名。

## 校验

- `00` 表 1,217 行、31 列，结构一致。
- `01` 表 138 行、7 列，结构一致。
- 所有尺寸精选外键均能关联到 `00` 表。
- B0 测试：9 项通过。
